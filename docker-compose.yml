services:
  backend:
    build: ./backend
    environment:
      ARBVIZ_CORS_ORIGINS: http://localhost:5173
    ports: ["8000:8000"]
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
